<%= form_for(@creation) do |f| %>
  <% if @creation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creation.errors.count, "error") %> prohibited this creation from being saved:</h2>

      <ul>
      <% @creation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= collection_select(:creation, :topic_id, Topic.all, :id, :name ) %>
  </div>
  <div class="field">
    <%= f.label :news %><br />
    <%= f.check_box :news %>
  </div>
  <div class="field">
    <%= f.label :kind_id %><br />
    <%= collection_select(:creation, :kind_id, Kind.all, :id, :name ) %>
  </div>
  <div class="field">
    <%= f.label :summary %><br />
    <%= f.text_area :summary %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

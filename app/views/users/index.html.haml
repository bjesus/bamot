%div.span-20
  - @garinim.each do |garin|
    %h2
      = garin.name
      %sub= garin.grade.name

    - garin.users.order('firstname').each do |user|
      .creation.space
        %h3
          = link_to(user.fullname, "/users/"+user.friendly_id)
